def parseSourceMediaFile(endpoint):
    pass


class SourceMediaFileWebScrapper:

    @classmethod
    def get_media_files(cls, endpoint):
        # get the page and search all video and audio tag, copy the href values,
        # and add it to a list
        parseSourceMediaFile(endpoint)
        return []
